# miinimal feed

This repo is the data store for a [miinimal](https://github.com/hyungsub08/miinimal) personal feed.
Posts live in `posts/*.md`; `feed.json` is the derived index, rewritten in the same commit as each post.
